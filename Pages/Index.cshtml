@page
@using offensive_fortune
@using Microsoft.EntityFrameworkCore
@inject IFortuneService FortuneService
@inject ApplicationDbContext DbContext

@{
    var fortune = await FortuneService.GetRandom2("/usr/share/games/fortunes/off/");
    fortune = fortune.Replace("\n", "<br>");
    var requestCount = await DbContext.RequestLogs.CountAsync();
}

<html>
<style>
    body {
  background-color: #101010;
  color: #ffffff;
  font-family: "Lucida Console", "Courier New", monospace;
  font-size: x-large;
}
div.text-center {
    padding-top: 10%;
      display: flex;
  justify-content: center;
  align-items: center;
}
</style>
<head>
    <title>Offensive</title>
</head>
<div class="text-center">
@Html.Raw(fortune)
</div>
<!-- Total requests logged: @requestCount -->
</html>
